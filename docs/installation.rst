============
Installation
============

At the command line::

    $ easy_install django-modelstats

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-modelstats
    $ pip install django-modelstats
